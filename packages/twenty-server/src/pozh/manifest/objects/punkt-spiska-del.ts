// пожсервис: пункт списка дел внутри задачи.
//
// ЗАЧЕМ. Обслуживание огнетушителя — это не «сделать», а пять операций подряд:
// осмотр корпуса, проверка давления, перезарядка, пломба, отметка в журнале.
// Сейчас это либо пять отдельных задач — и доска забита однотипными карточками,
// либо одна задача, в которой половину забыли.
//
// Это единственное, чего у задач Twenty нет вовсе, а нам нужно заметно: в
// YouGile такие списки называются подзадачами и показывают «сделано 3 из 7».
//
// ПОЧЕМУ ОТДЕЛЬНЫЙ ОБЪЕКТ, А НЕ ТЕКСТ В ОПИСАНИИ. Текстом нельзя ни отметить
// выполненное, ни посчитать остаток, ни спросить «что не доделано по этому
// объекту». А спрашивать будут именно так.

import { type ObjectManifest } from 'twenty-shared/application';

import { ОБЪЕКТЫ } from 'src/pozh/manifest/pozh-ids';
import { источник, опознаватель, текст, флажок, число } from 'src/pozh/manifest/pozh-pole';

const О = ОБЪЕКТЫ.пунктСписка;

export const пунктСписка: ObjectManifest = {
  universalIdentifier: О,
  nameSingular: 'pozhChecklistItem',
  namePlural: 'pozhChecklistItems',
  labelSingular: 'Пункт списка дел',
  labelPlural: 'Список дел',
  description: 'Шаги внутри задачи: осмотр, проверка, перезарядка, отметка в журнале.',
  icon: 'IconListCheck',
  labelIdentifierFieldMetadataUniversalIdentifier: опознаватель(О, 'name'),
  fields: [
    текст({
      объект: О,
      имя: 'name',
      подпись: 'Что сделать',
      пояснение: 'Одно действие одной строкой: «проверить давление», а не «обслужить».',
      обязательно: true,
    }),
    флажок({
      объект: О,
      имя: 'isDone',
      подпись: 'Сделано',
      пояснение: 'Отметка ставится тем, кто выполнил. По ней считается «сделано 3 из 7».',
    }),
    число({
      объект: О,
      имя: 'order',
      подпись: 'Порядок',
      пояснение:
        'В каком порядке идут шаги. У обслуживания порядок не произвольный: '
        + 'пломба ставится после перезарядки, а не до.',
    }),
    ...источник(О),
  ],
};
