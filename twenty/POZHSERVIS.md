# Форк ПожСервиса: что здесь наше

Это форк Twenty 0.2.1 (коммит `b5826b2d` от 11.08.2026) для ООО «ПожСервис».
**Обновлений от разработчика мы не принимаем** — решение владельца, разбор в
[ADR-002](../docs/architecture/adr/0002-fork-bez-obnovlenij.md).

Этот файл существует ради одного: **через год никто не вспомнит, что здесь наше,
а что было изначально**, и будет бояться трогать всё подряд.

---

## Правило владельца: новая работа делается ЗДЕСЬ

> **ВСЕ НОВЫЕ ФУНКЦИИ ДЕЛАЮТСЯ НЕ В СТАРОМ ПРИЛОЖЕНИИ, А В TWENTY, ЧТОБЫ ВСЁ
> БЫЛО ПОДВЯЗАНО ТАМ ЧЕРЕЗ ИНТЕРФЕЙС.**

Сказано владельцем 12.08.2026. Повод: раздел кадров сделали целиком в службе
документов — свои таблицы, свои страницы, свой список слева, — а в Twenty он
попал только рамкой, да и та лежала в разделе «ведётся разработка». Со стороны
это выглядело как «перешли на Twenty, а работа идёт по-старому».

- Новая сущность (справочник, документ, запись) — **объект в манифесте**,
  `packages/twenty-server/src/pozh/manifest/`.
- Новый список, карточка, фильтр, связь — **штатными средствами Twenty**. Своего
  экрана не заводить.
- Показывать сделанное надо на `localhost:3001`, а не на `localhost:8000`.

**Единственное исключение** — экран, который Twenty нарисовать не может в
принципе (сетка «сто человек × тридцать один день», где щелчок по клетке ставит
отпуск). Решается не молча: сначала показать, почему нельзя, потом спросить
владельца, потом в тот же день добавить экран в меню рамки со своим разделом —
`views/embedded.py::РАЗРЕШЁННЫЕ` в службе документов.

Полностью правило — в `~/fire-safety-assistant/CLAUDE.md`, раздел 0.

---

## Правило пометок

Каждая наша правка внутри Twenty помечается словом **«пожсервис»** в комментарии
рядом и объяснением, зачем она. Найти всё наше:

```bash
grep -rn "пожсервис\|ПожСервис" packages --include=*.ts --include=*.tsx --include=*.html --include=*.css
```

---

## Что уже изменено

### 1. Запрет выхода в интернет (сервер)

| | |
|---|---|
| Новый файл | `packages/twenty-server/src/pozh-netguard.ts` |
| Тронут | `packages/twenty-server/src/main.ts` — вызов ПЕРВОЙ строкой |
| Тесты | `packages/twenty-server/src/__tests__/pozh-netguard.spec.ts` (24) |

**Зачем.** В договорах с ПАО НЛМК раздел о конфиденциальности со штрафом.
Поводом стала находка: телеметрия Twenty **включена по умолчанию** и шлёт
события о работе на `twenty-telemetry.com`.

**Почему в коде, а не настройкой.** Настройка — обещание, а не гарантия: её
забудут перенести на новый сервер, её вернёт новая возможность. Запрет в коде
позволяет сказать заказчику не «мы выключили», а «оно не может».

**Как отключить на время наладки:** `POZH_ALLOW_INTERNET=1`.
**Как разрешить конкретный адрес:** `POZH_ALLOWED_HOSTS=имя1,имя2` (поддомены
разрешаются вместе с именем).

### 2. Запрет выхода в интернет (браузер)

| | |
|---|---|
| Тронут | `packages/twenty-front/index.html` |

Политика содержимого сводит всё к своему адресу. Убраны две картинки для
соцсетей, тянувшиеся с `raw.githubusercontent.com`.

**Долг:** `'unsafe-inline'` и `'unsafe-eval'` оставлены ради режима разработки.
**На боевом сервере их надо убрать.**

### 3. Фирменные цвета

| | |
|---|---|
| Новый файл | `packages/twenty-front/public/pozh-theme.css` |
| Тронут | `packages/twenty-front/index.html` — одна строка подключения |

Ставится накладкой из `/Users/artemharitonov/pozh-crm/branding/`. Пересборка не
нужна: Twenty читает цвета из переменных стилей во время работы.

### 4. Настройки

`packages/twenty-server/.env` — раздел «ПожСервис»:

| Настройка | Зачем |
|---|---|
| `TELEMETRY_ENABLED=false` | второй слой поверх запрета в коде |
| `ANALYTICS_ENABLED=false` | то же |
| `IS_BILLING_ENABLED=false` | оплата подписок нам не нужна |
| `CODE_INTERPRETER_TYPE=DISABLED` | **важно:** в исходном Twenty интерпретатор кода ВКЛЮЧЁН в режиме разработки. Наш агент не должен уметь выполнять произвольный код никогда |
| `POZH_ALLOWED_HOSTS=` | пусто: разрешение выдаётся поимённо |

### 5. Экраны службы документов внутри Twenty

| | |
|---|---|
| Новые файлы | `packages/twenty-server/src/pozh/` — ручка выдачи пропуска и её проверки |
| | `packages/twenty-front/src/pages/pozh/PozhScreenPage.tsx` — страница-рамка |
| | `packages/twenty-front/src/modules/navigation/components/PozhNavigationSection.tsx` — раздел меню |
| Тронуто | `twenty-shared/src/types/AppPath.ts` — адрес страницы |
| | `twenty-shared/src/types/ApiPath.ts` — путь ручек (чтобы его проксировали) |
| | `useCreateWorkspaceAppRouter.tsx`, `MainNavigationDrawerScrollableItems.tsx` |
| | `twenty-front/index.html` — разрешение на рамку в политике содержимого |
| Тесты | `packages/twenty-server/src/pozh/__tests__/pozh-adresa.spec.ts` (3) |

**Зачем.** Двадцать девять экранов остаются в службе на Python (счёт на
13.08.2026, пересчитан по коду службы: `views/embedded.py`, список
`РАЗРЕШЁННЫЕ`). Переписывать их значило бы выбросить не код, а примерно
шестьсот проверок, написанных на настоящих реквизитах компании. Twenty даёт
рамку и меню.

**Список экранов спрашивается у службы**, а не хранится копией. Копия здесь
была и называлась «вторым замком» — это было преувеличением: файлы отдаёт
только служба. Копия могла лишь разойтись с оригиналом.

---

## Правило, за которое уже трижды платили

**В адресе — латиница. Русский только там, где его читает человек.**

Кириллица в адресе ломается МОЛЧА, и каждый раз по-новому:

| Где | Как выглядело |
|---|---|
| Имя параметра `пропуск` | сервер отвечал «Invalid HTTP request» |
| Путь в клиенте на Python | падение при кодировании |
| `@Get('экраны')` в Nest | маршрут в списке есть, запрос получает 404 |

Причина одна: браузер кодирует кириллицу всегда, а сверка адреса идёт до
раскодирования. Служба на Python исключение — она раскодирует до сверки,
поэтому её адреса остались русскими.

На это стоит замок: `pozh-adresa.spec.ts` читает исходники с диска.

### 6. Учёт компании: объекты, права, перевозка данных

| | |
|---|---|
| Новые файлы | `packages/twenty-server/src/pozh/manifest/` — описание 14 объектов, указатели, роли, пункты меню |
| Выкладка | `npx tsx --tsconfig tsconfig.json src/pozh/manifest/pozh-sync.ts [--просмотр]` |

**Порядок работ обязательный:** снимок базы → `--просмотр` → выкладка. При
выкладке включён режим «чего нет в описании, того нет и в базе»: убранный по
недосмотру объект уносит таблицу вместе с данными.

**Права.** Две роли: «Сотрудник ПожСервиса» (персональных данных водителей не
видит) и «Кадры и путевые листы». Удаление записей не выдано никому — см. ниже
почему.

---

## Три ловушки Twenty, за которые уже заплачено

Все три молчаливые: ошибки нет, в журналах чисто, а работает не так.

### Удалённая запись не исчезает и мешает

Запись уезжает в корзину, но свою отметку «приехала оттуда-то» держит. Повторная
перевозка получает «такая уже есть» и пропускает — **а в списке пусто, и запись
не появится никогда.** Она же не даёт построить уникальный указатель.

Отсюда решение: удаление записей не выдано обычным сотрудникам вовсе.

### Кириллица в адресе ручки

Маршрут значится в списке, а запрос получает 404: сверка идёт до
раскодирования, а браузер кириллицу кодирует всегда. Правило: **в адресе —
латиница, русский только в том, что видит человек.** Замок:
`src/pozh/__tests__/pozh-adresa.spec.ts`.

### Уникальность «на всю базу» там, где организаций две

Номер путевого листа был уникален глобально. «ПожСервис» и «ПожМастер»
нумеруют листы каждый по-своему, и второй лист с тем же номером просто не
записался бы. Стало: уникальна тройка «организация + серия + номер».

Это стоит проверять у каждого нового уникального поля.

---

## Долги, которые надо закрыть

- [ ] убрать `'unsafe-inline'` и `'unsafe-eval'` из политики содержимого на бою;
- [ ] отдельно проверить логические функции — второй способ, которым агент может
      обойти свои ограничения;
- [ ] завести раз в квартал просмотр исправлений безопасности в Twenty и перенос
      нужного точечно (см. ADR-002 — это цена форка);
- [x] ~~в политике содержимого адрес службы прописан как `localhost:8000`~~ —
      закрыто: подставляется при сборке из `REACT_APP_POZH_DOCS_URL`, на это
      стоит проверка;
- [ ] логотип, название продукта и значок вкладки — ставятся настройкой из
      интерфейса, руки не дошли.

---

## Чего здесь НЕ надо делать

- **Не запускать `git pull` из Twenty.** Мы форк, а не копия: наши правки
  разъедутся с чужими без всякой пользы.
- **Не убирать запрет сети «чтобы заработало».** Если что-то перестало работать
  из-за него — значит оно ходило наружу, и надо понять, куда именно, а не
  снимать защиту.
- **Не править цвета в исходниках темы.** Это уже пробовали: работающая
  программа берёт их из переменных стилей, а правка исходника требует пересборки
  и всё равно перекрывается накладкой.
