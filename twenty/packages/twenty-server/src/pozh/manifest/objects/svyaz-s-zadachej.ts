// пожсервис: связь списка дел с задачей Twenty.
//
// Задача — объект не наш, он встроенный. Опознаватель у встроенных объектов
// постоянный, и привязаться к нему из своего описания можно — это проверено
// предварительным просмотром, а не предположено.
//
// ПОЧЕМУ ПРИ УДАЛЕНИИ «ПО ЦЕПОЧКЕ». Пункт списка дел сам по себе не значит
// ничего: «проверить давление» без задачи, к которой это относится, — мусор в
// базе. Удалили задачу — её шаги уходят следом.

import { type ObjectFieldManifest } from 'twenty-shared/application';
import { RelationOnDeleteAction } from 'twenty-shared/types';

import { ОБЪЕКТЫ } from 'src/pozh/manifest/pozh-ids';
import { связь } from 'src/pozh/manifest/pozh-pole';

/** Встроенная задача Twenty. Опознаватель взят из работающей системы. */
const ЗАДАЧА = '20202020-1ba1-48ba-bc83-ef7e5990ed10';

const списокДелЗадачи = связь({
  откуда: ОБЪЕКТЫ.пунктСписка,
  куда: ЗАДАЧА,
  имяСсылки: 'task',
  подписьСсылки: 'Задача',
  имяСписка: 'pozhChecklistItems',
  подписьСписка: 'Список дел',
  приУдалении: RelationOnDeleteAction.CASCADE,
  пояснение: 'К какой задаче относится этот шаг.',
});

export const связиСЗадачей: ObjectFieldManifest[] = [
  списокДелЗадачи.ссылка,
  списокДелЗадачи.список,
];
