# Как поднять и посмотреть

## Быстро

```bash
cd /Users/artemharitonov/pozh-crm/twenty
export PATH="/opt/homebrew/opt/node@24/bin:$PATH"

redis-server --daemonize yes --port 6379     # если ещё не запущен
npx nx start twenty-server                    # окно 1, порт 3000
npx nx start twenty-front                     # окно 2, порт 3001
```

Открыть: **http://localhost:3001**

Демонстрационный вход: `tim@apple.dev` (пространство Apple) или
`phil@apple.dev`. Пароль подставляется сам — в настройках стоит
`SIGN_IN_PREFILLED=true`.

---

## Что уже стоит на машине

| | |
|---|---|
| Node 24.19.0 | `/opt/homebrew/opt/node@24/bin` — отдельно от системного node 26, тот не тронут |
| Yarn 4.13.0 | через corepack |
| PostgreSQL 18.4 | база `default`, роль `postgres`, 133 таблицы |
| Redis 8.10.0 | порт 6379 |
| Зависимости Twenty | 3,9 ГБ в `twenty/node_modules` |

**Twenty требует node 24.x.** Без строки `export PATH=...` возьмётся системный
26, и запуск упадёт с невнятной ошибкой.

---

## Фирменные цвета

```bash
cd /Users/artemharitonov/pozh-crm
node branding/apply-branding.mjs             # поставить
node branding/apply-branding.mjs --проверить # показать, что сделает
node branding/apply-branding.mjs --снять     # убрать
```

**Пересобирать ничего не нужно** — Twenty читает цвета из переменных стилей во
время работы. Обновите страницу в браузере.

Проверить, что подействовало (консоль браузера):

```js
getComputedStyle(document.documentElement).getPropertyValue('--t-color-blue')
```

Должно вернуть `#e31e24`, а не синий.

### Когда накладку надо ставить заново

Накладка — это не настройка внутри программы, а два изменения в файлах папки
`twenty/`: файл стилей кладётся в `packages/twenty-front/public/`, а ссылка на
него дописывается в `index.html`. Перезаписали эти два места — накладки больше
нет, надо прогнать команду снова.

Раньше здесь стояла оговорка «после `git pull` в Twenty», и она устарела дважды.
Во-первых, обновления от авторов Twenty мы не берём вовсе — решение владельца,
файл `docs/architecture/adr/0002-fork-bez-obnovlenij.md`. Во-вторых, с
13.08.2026 у папки `twenty/` нет своего склада кода: она лежит внутри склада
`pozh-crm`, и забирать правки надо из корня проекта, а не изнутри `twenty/`:

```bash
cd ~/pozh-crm && git pull        # забрать свежие правки из своего склада
```

Поводов поставить накладку заново теперь ровно два:

- забрали свежие правки из своего склада, и среди них были правки `index.html`
  или содержимого `packages/twenty-front/public/`;
- развернули проект заново — на другом компьютере или на боевом сервере.

## Русский язык интерфейса

Часть перевода лежит в файлах и приезжает сама через git (словари,
исправленные места в коде Twenty). Другая часть — **записи в базе данных
этого пространства**: названия объектов («Сделки» вместо «Opportunities»),
названия видов списка, папка меню, подписи команд («Новая запись» вместо
«New»). Свежая база — хоть на новом компьютере, хоть на боевом сервере после
восстановления — этих записей не содержит вовсе, и без следующего шага
интерфейс снова станет наполовину английским.

```bash
cd /Users/artemharitonov/pozh-crm
export PATH="/opt/homebrew/opt/node@24/bin:$PATH"

# Токен — из браузера после входа администратором:
# localStorage.getItem('tokenPairState') → accessOrWorkspaceAgnosticToken.token
# На боевом сервере лучше постоянный ключ: Настройки → API keys.

TWENTY_API_TOKEN=... node branding/provision-ru-locale.mjs             # поставить
TWENTY_API_TOKEN=... node branding/provision-ru-locale.mjs --проверить # показать, что сделает
```

Прогонять безопасно повторно: скрипт сначала смотрит, что уже верно, и трогает
только расхождение. Каждая запись находится по имени, а не по номеру — на
боевом сервере, где база поднята заново, номера будут другими, а имена те же.

**Если правки не видны сразу** — сервер держит часть этих записей в памяти.
Обновление приходит через минуту-другую само; ускорить можно, перезапустив
`twenty-server` после прогона скрипта.

**Чего этот скрипт не чинит, и почему** — у своих объектов (Контрагенты,
Огнетушители…) четыре графы «Кто создал», «Когда создал», «Кто изменил»,
«Когда изменил» остаются английскими. Сервер Twenty намеренно отказывается
менять подпись у этих граф с явной ошибкой защиты — это решение разработчиков
Twenty, а не наша недоделка, и обходить его правкой базы напрямую мы не стали.

## Секретное слово, которым связаны CRM и служба документов

Экраны службы (кадры, путевые листы, договоры) открываются внутри окна CRM.
Чтобы служба поверила, что человек пришёл именно из CRM, а не постучался с
улицы, у двух программ есть одно общее секретное слово. Оно нигде не
показывается человеку и нужно только им двоим.

Хранится с двух сторон:

| Сторона | Где лежит |
|---|---|
| CRM | строка `POZH_GATE_KEY=` в файле настроек `twenty/packages/twenty-server/.env` |
| Служба документов | файл `gate_key.txt` в папке данных: на боевом сервере это `C:\ProgramData\PozhAI`, на ноутбуке `~/.local/share/assistent-pb` |

Слова должны совпадать дословно. Не совпадают или не заданы — экраны службы
внутри CRM не открываются, а человек видит «Связь с проверкой документов не
настроена».

**На ноутбуке разработчика делать ничего не нужно** — проверено 13.08.2026 и
ещё раз при этой правке: слово задано с обеих сторон и совпадает. Сверялись
короткие отпечатки, сами значения не выводились и никуда не записаны; отпечаток
здесь тоже не приводится — он ничего не даёт человеку, но помогает тому, кто
подбирает слово.

**На боевом сервере его, скорее всего, нет:** установщик такую настройку не
задаёт (риск №8 карты работ). Задаётся оно так, чтобы никто, включая меня, его
не увидел: слово рождается случайным прямо на сервере и сразу уходит в оба
файла, не попадая ни в вывод на экран, ни в переписку, ни в историю правок
(файл настроек CRM в склад кода не сохраняется, а файл службы лежит вообще вне
папки с кодом).

PowerShell на сервере, от администратора, **одним куском** — если выполнить по
частям, слово потеряется между командами:

```powershell
# 1. Слово рождается здесь же и живёт только внутри этого окна PowerShell.
$байты = New-Object byte[] 32
[Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($байты)
$слово = -join ($байты | ForEach-Object { $_.ToString('x2') })

# 2. Сторона службы документов. Кодировка задаётся руками и без метки в начале
#    файла: Set-Content в Windows PowerShell дописывает такую метку, служба
#    считает её частью слова, и стороны молча перестают сходиться.
$папка = "$env:ProgramData\PozhAI"
New-Item -ItemType Directory -Force -Path $папка | Out-Null
[IO.File]::WriteAllText("$папка\gate_key.txt", $слово, (New-Object Text.UTF8Encoding $false))

# 3. Сторона CRM. Прежняя строка убирается, чтобы их не стало две.
#    Путь — до того места, куда на сервере распакована CRM; поправить под себя.
$настройки = "C:\pozh-crm\twenty\packages\twenty-server\.env"
$строки = @(Get-Content -LiteralPath $настройки | Where-Object { $_ -notmatch '^\s*POZH_GATE_KEY\s*=' })
$строки += "POZH_GATE_KEY=$слово"
[IO.File]::WriteAllLines($настройки, $строки, (New-Object Text.UTF8Encoding $false))

# 4. Слово стирается из памяти окна. На экран оно не выводилось ни разу.
Remove-Variable слово, байты
```

После этого **перезапустить `twenty-server`**: настройки он читает при старте.
Службе документов перезапуск не нужен — она заглядывает в свой файл каждый раз.

Проверить, что стороны сошлись, можно не показывая слово: сравниваются короткие
отпечатки — по ним значение не восстановить, а расхождение видно сразу.

```powershell
$отпечаток = {
  param($текст)
  $х = [Security.Cryptography.SHA256]::Create().ComputeHash([Text.Encoding]::UTF8.GetBytes($текст))
  (-join ($х | ForEach-Object { $_.ToString('x2') })).Substring(0, 12)
}
$изФайла = (Get-Content -Raw "$env:ProgramData\PozhAI\gate_key.txt").Trim()
$изCRM = ((Select-String -Path "C:\pozh-crm\twenty\packages\twenty-server\.env" `
  -Pattern '^\s*POZH_GATE_KEY\s*=' | Select-Object -First 1).Line -split '=', 2)[1].Trim()
"служба: $(& $отпечаток $изФайла)"
"CRM:    $(& $отпечаток $изCRM)"
```

Две строки должны совпасть. Совпали — связь настроена; разошлись — прогнать
первый кусок заново целиком.

**Если слово там уже задано и отпечатки сходятся — не трогать.** Замена рабочего
слова ничего не улучшает, а на время перезапуска экраны службы внутри CRM
перестают открываться.

## Если что-то не поднимается

| Признак | Причина |
|---|---|
| Ошибка про версию node | не выставлен PATH на node@24 |
| Сервер не стартует, жалуется на подключение | не запущен redis или postgres |
| Порт занят | `pkill -f "nx start"` и заново |
| Интерфейс белый | подождать: первая сборка страницы занимает время |
| Цвета не поменялись | не прогнана накладка, либо не обновлена страница |

Логи прошлого запуска: `/tmp/twenty-server.log`, `/tmp/twenty-front.log`,
`/tmp/twenty-db2.log`.

---

## Ограничение по железу

Памяти на машине **8 ГБ**. Сборка `twenty-ui` в неё укладывается, полная сборка
фронта не пробовалась. Если что-то падает с нехваткой памяти — уменьшать
`--max-old-space-size`, а не увеличивать: своп сделает только хуже.

---

## Как сохранить работу

С 13.08.2026 папка `twenty/` — часть склада кода `pozh-crm`, своего отдельного
склада у неё больше нет. Раньше сохранять и отправлять приходилось дважды: одну
команду в корне проекта, вторую — внутри `twenty/`. Теперь это одна команда, и
выполняется она **из корня `~/pozh-crm`**.

Сама команда самопроверки и разбор того, что она печатает, — в
[ONBOARDING.md](ONBOARDING.md), раздел 5 «Как сохранять работу». Здесь её
намеренно нет: в двух местах команды рано или поздно разойдутся.

У службы документов (`~/fire-safety-assistant`) склад кода свой, отдельный, и
сохраняется она отдельно, в своей папке.

---

## Старое приложение продолжает работать

`/Users/artemharitonov/fire-safety-assistant` не тронут и работает как работал.
По [стратегии переезда](docs/migration/MIGRATION_STRATEGY.md) люди пользуются им,
пока функция не перенесена; перенесли — выключаем в старом, а не оставляем «на
всякий случай».

---

## На Windows: три ямы, каждая из которых выглядит как поломка проекта

Раздел выше написан под Mac Артёма. На машине владельца (Windows) проект тоже
поднимается, но по дороге стоят три ловушки. Все три дают отказ, из которого
никак не следует настоящая причина, — поэтому они здесь.

Замерено 15.08.2026 на чистом клоне.

### 1. «Слишком длинное имя файла» при загрузке

У Twenty пути под 300 знаков, а Windows по умолчанию пускает 260. Клон при этом
формально проходит, но половина файлов не распаковывается, и `git status`
показывает двадцать восемь тысяч «удалённых» файлов. Выглядит как испорченный
репозиторий.

```bash
git config core.longpaths true
git reset --hard HEAD
```

Второй командой собирается опись файлов, которую снесли оборвавшиеся попытки.

### 2. Сборка висит навсегда, не загружая процессор

`yarn nx build twenty-shared` останавливается на шаге `generateBarrels` и стоит
часами: процессы живут, процессор не загружен, вывода нет.

Причина — форматировщик кода в синхронной обёртке (`@prettier/sync`): на Windows
он запускает рабочий поток, который не отвечает.

**Обход: этот шаг вообще не нужен.** Он пересоздаёт тридцать точек входа
(`index.ts`) общего пакета, а они уже лежат в репозитории готовыми. Сборку можно
запустить напрямую, минуя его:

```bash
cd packages/twenty-shared
npx vite build
```

Проверкам этого достаточно: им нужны `dist/*.cjs`, а не описания типов.

### 3. Любая проверка сервера падает с «не найден twenty-shared»

Следствие второй ямы, но выглядит совершенно иначе — как будто сломан импорт.
`yarn install` при этом проходит чисто, и человек ищет причину в своём коде.

Порядок обязательный: **установить зависимости → собрать `twenty-shared` →
только потом прогонять проверки.**

```bash
cd packages/twenty-server
CI=true NX_DAEMON=false npx jest src/pozh/__tests__/
```

Переменные не для красоты: без них nx при первом запуске задаёт вопрос про своё
облако и в фоновом запуске виснет на нём молча.

### Чего на Windows пока нет

Проверки идут, **саму CRM в браузере поднять ещё нельзя**: для этого нужны
PostgreSQL и Redis, а они не установлены. Это отдельный шаг.

### Что поставлено на машине владельца

| | |
|---|---|
| Node 24.19.0 | winget, `C:\Program Files\nodejs` |
| Yarn 4.13.0 | corepack, но в `C:\Users\Windows\.corepack-bin` — штатная установка требует прав администратора и упирается в отказ на `Program Files` |
| Зависимости Twenty | `twenty/node_modules`, ставятся 8 минут |

### Ещё четыре ямы, найденные при первом полном запуске (16.08.2026)

**4. Пространство остаётся в состоянии «создаётся».** `workspace:seed:dev`
отрабатывает с ненулевым кодом возврата, хотя миграции проходят и пользователи
появляются. Пространство остаётся `PENDING_CREATION`, и экран показывает
«Не удалось создать рабочее пространство» — то есть предлагает достроить его и
не может. Лечится:

```sql
UPDATE core.workspace SET "activationStatus" = 'ACTIVE';
```

**5. Выкладка описи падает с «File not found».** Установка читает
`dependencies/package.json` приложения в хранилище (`.local-storage`), а при
заведении приложения эта папка не создаётся. Берётся по образцу любого готового
приложения из соседней папки.

**6. Служебные команды работают только в режиме разработки.** `NODE_ENV` по
умолчанию боевой, и `workspace:generate-api-key` отвечает «This command is only
available in development or test environments». Ключ доступа нужен приёмнику
нарядов, поэтому без него обмен не настроить.

**7. Названия объектов и полей лежат в базе ТЕКСТОМ.** Переключение языка их не
трогает: кнопки становятся русскими, а в меню остаются «Companies» и «Tasks».
Лечится файлом `pozh-perevod.ts` — запускать ПОСЛЕ каждой выкладки описи и
после каждого `database:reset`, иначе всё возвращается на английский.

Язык при этом хранится в трёх местах, и экран берёт его у сотрудника рабочего
пространства. Поставленный только пользователю не даёт ничего.

### Порядок полного развёртывания с нуля

```bash
git config core.longpaths true
yarn install
cd packages/twenty-shared && npx vite build && cd ../..
cd packages/twenty-client-sdk && npx vite build && npx vite build -c vite.metadata.config.ts && cd ../..
cd packages/twenty-emails && npx vite build && cd ../..
cd packages/twenty-sdk && for f in node define billing front-component logic-function utils browser; do npx vite build -c vite.config.$f.ts; done && cd ../..
cd packages/twenty-server && npx nest build --path ./tsconfig.build.json
node dist/database/scripts/setup-db.js
node dist/command/command.js run-instance-commands --force
NODE_ENV=development node dist/command/command.js workspace:seed:dev
# затем: пространство в ACTIVE, папка приложения в .local-storage,
# npx tsx src/pozh/manifest/pozh-sync.ts --просмотр && npx tsx src/pozh/manifest/pozh-sync.ts
# npx tsx src/pozh/manifest/pozh-perevod.ts
```

Ни один шаг здесь нельзя запускать через `nx` — см. яму 2.
