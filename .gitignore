node_modules/
.env
.env.*
!.env.example
__pycache__/
.DS_Store
*.log
twenty/.git-arhiv-do-slyaniya/

# Резервные копии баз: внутри персональные данные и договоры — наружу нельзя
backups/
*.dump
core-schema-*.sql

# Сохранённое состояние Redis. Кладётся рядом с кодом самим Redis, к работе
# отношения не имеет и меняется постоянно.
#
# ПОЧЕМУ ЭТО ВАЖНЕЕ, ЧЕМ КАЖЕТСЯ. Пока файл не в этом списке, git считает его
# «несохранённым изменением», а обновление на грязной рабочей копии не идёт
# вовсе — оно бы стёрло чужую работу. То есть один посторонний файл тихо
# останавливал обновление всей CRM на сервере. В складе службы документов
# такие файлы игнорируются с самого начала, здесь строку просто забыли.
dump.rdb
